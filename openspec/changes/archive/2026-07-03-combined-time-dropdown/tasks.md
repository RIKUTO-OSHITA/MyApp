## 1. 時刻ユーティリティの拡張

- [x] 1.1 `src/utils/timeSlots.js`に、`DAY_START_TIME`〜`DAY_END_TIME`を15分刻みで列挙した「HH:MM」文字列一覧を返す関数（`listDayTimes()`）を追加する。タイムライン描画用の`SLOT_MINUTES`（30分）とは独立した選択粒度の定数を使う。
- [x] 1.2 追加した関数のユニットテストを`tests/timeSlots.test.js`に追加する（件数・先頭値・末尾値・刻み幅を検証）。
- [x] 1.3 時刻フォーマットのバリデーション（`normalizeTaskInput`が使う書式チェック）を、30分刻みのみの許容から15分刻み（分が00/15/30/45）の許容に変更する。

## 2. 時刻プルダウンUIの実装

- [x] 2.1 `index.html`の`#start-time`・`#end-time`を`<input type="time">`から`<select>`に置き換える。
- [x] 2.2 `src/main.js`で、タスク追加・編集ダイアログを開く際に開始時刻・終了時刻の`<select>`へ選択肢を動的に挿入する処理を実装する。
- [x] 2.3 開始時刻`<select>`の`change`イベントで、終了時刻`<select>`の選択肢を「開始時刻より後の時刻のみ」に絞り込んで再生成する処理を実装する。既存の終了時刻選択が絞り込み後も有効なら維持し、無効なら開始時刻の15分後を既定値にする。
- [x] 2.4 新規作成（ボタン起点）・スロットダブルクリック起点・既存タスク編集起点のそれぞれで、開始・終了時刻の初期選択値が正しく設定されることを確認・調整する。
- [x] 2.5 15分刻みで選択した開始・終了時刻が、30分刻みのタイムライン表示グリッド上でも正しい位置・高さで描画されることを確認する（グリッド行の数・ラベルは30分刻みのまま変わらないこと）。

## 3. スタイル調整

- [x] 3.1 `src/styles/app.css`で`<select>`要素に既存の`#task-form input`と統一感のあるボーダー・角丸・パディング・フォントを適用する。

## 4. テスト・ビルド更新

- [x] 4.1 `tests/scheduler-ui-flow.test.js`など、時刻入力に依存する既存テストを新しい選択方式に合わせて更新する。
- [x] 4.2 `npm test`を実行し、全テストが成功することを確認する。
- [x] 4.3 `npm run build`を実行し、`dist/index.html`を最新の実装で再生成する。
- [x] 4.4 ブラウザ（`dist/index.html`をfile://で開く）でタスク追加・編集・削除、および開始時刻変更に応じた終了時刻選択肢の絞り込みが正しく動作することを手動確認する。
